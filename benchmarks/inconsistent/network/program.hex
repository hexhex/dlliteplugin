
go(X,Y):- go(Y,X).

route(X,Y):-open(X), open(Y), &rDL["inst_prop_002_inst_000.owl",a,b,c,d,"edge"](X,Y).

go(X,Y):-route(X,Y), not &rDL["inst_prop_002_inst_000.owl",pc,b,c,d,"forbid"](X,Y).

open(X) v block(X):-not &cDL["inst_prop_002_inst_000.owl",a,b,c,d,"-Avail"](X), node(X).

negis(X):-domain(X), go(X,Y), X!=Y.

:-node(X), not negis(X).

pc("Block",X):-block(X).
